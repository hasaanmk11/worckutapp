import 'package:flutter/material.dart';

class adminLoginText extends StatelessWidget {
  const adminLoginText({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text(
      "Admin Login",
      style: TextStyle(
        fontSize: 28,
        fontWeight: FontWeight.bold,
        color: Colors.white,
      ),
    );
  }
}
